# Coach avatar (TalkingHead)

- File: `coach.glb` (Ready Player Me–compatible, Oculus visemes)
- Used by: `@met4citizen/talkinghead` via `/avatars/coach.glb`
- Sample currently shipped: TalkingHead demo `brunette.glb` (CC BY-NC — replace for commercial use)

Lip sync:
- Speaking: free edge-tts (`/api/tts`) + word timings → TalkingHead `speakAudio`
- Listening: audio file + energy-based visemes (transcript stays server-side)
