# LEARN brand assets

Official LEARN mark (book + pencil + wordmark).

| File | Use |
| --- | --- |
| `learn-lockup-*.svg` | Stacked logo (auth card) |
| `learn-lockup-horizontal-*.svg` | Nav / sidebar |
| `learn-mark-*.svg` | Icon-only |
| `*-black.svg` | Light backgrounds |
| `*-white.svg` | Dark UI chrome |

Prefer `BrandLogo` from `components/brand/BrandLogo.tsx` instead of hardcoding paths.
